    // for (let key in questionBlock) {
    //     console.log(questionBlock[key].hasOwnProperty('passage'))
    //     if (questionBlock[key].hasOwnProperty('passage')) {
    //         document.getElementsByClassName('root')[0].innerHTML += questionBlock[key].passage
    //         for (let q in questionBlock.questionBlock) {
    //             // console.log('here', Number(q) + 1)
    //             // console.log(questionBlock.questionBlock[q].a11y);
    //             document.getElementsByClassName('root')[0].innerHTML += questionBlock.questionBlock[q].a11y;
    //             document.getElementsByClassName('root')[0].innerHTML += `<div class="test-question question_holder" role="region" aria-label="Question">
    //             <div class="displayQuestion">
    //                 <span class="questionNumber">
    //                     <span class="offscreen">Question </span>
    //                     <span id="$questionNum">${Number(q) + 1}.</span>
    //                 </span>

    //                 <div class="flex_right">
    //                     ${questionBlock.questionBlock[q].question}
    //                 </div>
    //             </div>`
    //             // console.log(questionBlock.questionBlock[q])
    //         }
    //         console.log(questionNum, questionBlock.questionBlock)
    //     }

    // }




    //
        for (let key in questionBlock) {
        if (questionBlock[key].hasOwnProperty('passage')) {
            console.log('with passage')
            document.getElementsByClassName('root')[0].innerHTML = setHtmlWPassage();
            document.getElementsByClassName('passage_holder')[0].innerHTML += questionBlock[key].passage
            break;
        } else {
            console.log('without passage')
            document.getElementsByClassName('root')[0].innerHTML = setHtml();
            break;
        }
    }

    for (let q in questionBlock.questionBlock) {
        console.log('questions : ', Number(q) + 1)
        console.log(questionBlock.questionBlock[q])
        document.querySelector('[aria-label="Orientation Information"]').innerHTML = questionBlock.questionBlock[q].a11y;
    }



    // function correct() {
//     let questionNum = Number(localStorage.getItem('qNum'));
//     let questionBlock = getQuestionData(questionObject);
//     // console.log('insde correct', questionBlock.questionBlock)
//     let arr = questionBlock.questionBlock.map(el => el.correct)
//     let ol = document.querySelectorAll('data')
// }